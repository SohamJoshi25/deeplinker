<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title> 
</head>
<body>
    <%if (link) { %>
        <h1>Redirecting</h1>
    <% } else { %>
        <h1>Result</h1>
        <% }  %>

    <%if (url) { %>
        <h2 href="<%= url %>"><%= url %></h2>
    <% } %>
    <%if (link) { %>
        <a id="redirectLink" href="<%= link %>"><%= link %></a>
    <% }  %>

    <script>
        window.onload = function() {
        setTimeout(function() {
            document.getElementById('redirectLink').click();
        }, 10);
    };
    </script>
</body>
</html>